<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/map_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/pa_background">

    <!-- Google Map host (replaced by SupportMapFragment) -->
    <FrameLayout
        android:id="@+id/map_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Top-right FAB column: Reminder, My Location, Filter -->
    <LinearLayout
        android:id="@+id/top_right_fabs"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_gravity="top|end"
        android:layout_margin="16dp"
        android:elevation="1000dp">

        <!-- Parking reminder -->
        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fabSetReminder"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="8dp"
            android:contentDescription="@string/set_parking_reminder"
            app:fabSize="mini"
            app:shapeAppearanceOverlay="@style/RoundCorners50"
            app:srcCompat="@drawable/ic_alarm_24"
            app:backgroundTint="@color/pa_selected_tab"
            app:tint="@android:color/white" />

        <!-- My location -->
        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fabMyLocation"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="8dp"
            android:contentDescription="@string/my_location"
            app:fabSize="mini"
            app:shapeAppearanceOverlay="@style/RoundCorners50"
            app:srcCompat="@android:drawable/ic_menu_mylocation"
            app:backgroundTint="@color/pa_selected_tab"
            app:tint="@android:color/white" />

        <!-- Filter sightings -->
        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fabFilter"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:contentDescription="@string/filter_sightings"
            app:fabSize="mini"
            app:shapeAppearanceOverlay="@style/RoundCorners50"
            app:srcCompat="@drawable/ic_filter_list_24"
            app:backgroundTint="@color/pa_selected_tab"
            app:tint="@android:color/white" />
    </LinearLayout>

    <!-- Bottom-center FAB: opens the report dialog -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabReport"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="24dp"
        android:contentDescription="@string/report_sighting"
        app:backgroundTint="@color/pa_selected_tab"
        app:fabCustomSize="74dp"
        app:shapeAppearanceOverlay="@style/RoundCorners50"
        app:maxImageSize="32dp"
        app:srcCompat="@drawable/ic_plus"
        app:tint="@android:color/white" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>